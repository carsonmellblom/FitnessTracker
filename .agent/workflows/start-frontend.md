---
description: Start the React frontend dev server
---
// turbo-all

Start the FitnessTracker React frontend:

1. Navigate to the web project and start the dev server:
```powershell
cd d:\dev\FitnessTracker\src\FitnessTracker.Web
npm run dev
```

The frontend will be available at: http://localhost:5173
